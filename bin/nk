#!/usr/bin/env python3
import sys
from pathlib import Path

root = Path(__file__).resolve().parents[1]
application = root / "app"
sys.path.insert(0, str(application if application.is_dir() else root))

from nk.cli import main


raise SystemExit(main())
